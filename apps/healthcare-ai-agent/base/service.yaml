apiVersion: v1
kind: Service
metadata:
  name: healthcare-ai-agent
  labels:
    app.kubernetes.io/name: healthcare-ai-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: healthcare-ai-agent
  ports:
    - name: http
      port: 80
      targetPort: http
